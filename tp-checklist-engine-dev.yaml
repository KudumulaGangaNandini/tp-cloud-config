keycloak:
  issuer-uri: ${KEYCLOAK_URL_AUTH}/auth/realms/
springdoc:
  version: '@springdoc.version@'
  api-docs:
    path: /checklist-engine/v3/api-docs
gateway:
  uri: ${GATEWAY_URI}
database:
  url: ${TP_MODELER_APP_MONGO_URI}
  name: techsophy-platform
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG