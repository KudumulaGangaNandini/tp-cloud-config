keycloak:
  issuer-uri: ${KEYCLOAK_URL_AUTH}/auth/realms/
springdoc:
  version: '@springdoc.version@'
  api-docs:
    path: checklist-modeler/v3/api-docs

gateway:
  uri: ${GATEWAY_URI}

database:
  url: ${TP_MODELER_APP_MONGO_URI}
  name: techsophy-platform
  
default:
  pagelimit: ${DEFAULT_PAGE_SIZE}

