beconnector:
  config:
    camunda:
      baseUrl: ${GATEWAY_URI}/workflow/v1
sxp:
  journeyTriggerBaseURL: ${SXP_JOURNEY_BASE_URL}
  contextEngineURL: ${CE_DYN_JOURNEY_URL}

springdoc:
  version: '@springdoc.version@'
  api-docs:
    path: /pci/v3/api-docs


gateway:
  uri: ${GATEWAY_URI}
keycloak:
  issuer-uri: ${KEYCLOAK_URL_AUTH}/auth/realms/
database:
  url: mongodb://tp_modeler:T3ch%21%23%40%5E%26o%2A@mongo-0.mongo:27017,mongo-1.mongo:27017/techsophy-platform?authSource=admin
  name: techsophy-platform

pci:
  audit:
    mongo:
      enabled: ${PCI_AUDIT_MONGO_ENABLED}
    file:
      enabled: ${PCI_AUDIT_FILE_ENABLED}
