gateway:
  uri:  ${GATEWAY_URI}
keycloak:
  issuer-uri: ${KEYCLOAK_URL_AUTH}/auth/realms/

database:
  url: ${TP_MODELER_APP_MONGO_URI}
  name: techsophy-platform

insta:
  insta_url: ${INSTA_TOKEN_URL:https://api.instahealthsolutions.com/kims}
  hospital_name: ${INSTA_HOSTPITAL_NAME:kims}
  customer_user_id: ${INSTA_customer_USER:techsophy}
  customer_user_password: ${CUSTOME_USER_PASSWORD:techsophy123}

springdoc:
  version: '@springdoc.version@'